export interface ModeCardProps {
  title: string;
  description: string;
  href: string;
  icon: string;
}

export function ModeCard(_props: ModeCardProps) {
  // Stub — landing mode selector card implemented in a later step.
  return null;
}
