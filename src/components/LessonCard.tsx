import type { Sign } from '@/lib/signs';

export interface LessonCardProps {
  sign: Sign;
}

export function LessonCard(_props: LessonCardProps) {
  // Stub — learn mode card with GIF + "Try it" implemented in a later step.
  return null;
}
