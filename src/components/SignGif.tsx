export interface SignGifProps {
  label: string;
  gifPath: string;
  size?: 'sm' | 'md' | 'lg';
}

export function SignGif(_props: SignGifProps) {
  // Stub — sign GIF renderer implemented in a later step.
  return null;
}
